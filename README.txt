An example mocha test project for scripts using the
revealing module pattern.

Install mocha globally:
$ npm install -g mocha

Install local dependencies (including assertion framework):
$ npm install

Run tests:
$ npm test